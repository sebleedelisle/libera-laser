# Acme USB DAC example

This directory demonstrates the package layout for the ABI v1 example plugin.
Copy each built native library to the path declared in `manifest.json`, add any
runtime dependencies beside it, then run `tools/package_plugin.py`.
